#inner-container.span8
  #inner-header.span8
    %h1.pull-left
      Fitness Apps
    #profile-content.span8
      #profile-details
        .api-integrators.span8
          %h3.pull-left
            Integrate with RunKeeper
          -if current_user.connected?
            / %a#api-connect.btn.btn-danger.pull-right{:href => "disconnect", method: :delete} disconnect
            #api-connect.btn.btn-danger.pull-right
              = link_to "disconnect", disconnect_path, method: :delete, :class => "awyeah"
          -else
            %a#api-connect.btn.btn-primary.pull-right{:href => "/auth/runkeeper"} connect

