#landing-hero.span12
  %img(src="/assets/images/long_run_2.jpg")
  %h1 Ready, Set, Get Started with RunLine
  - if current_user
    Signed in as #{current_user.name}
    \#{link_to "Sign out", signout_path}
  - else
    = link_to "Sign in with Twitter", "/auth/twitter"

